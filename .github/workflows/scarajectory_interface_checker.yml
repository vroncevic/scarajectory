name: scarajectory_interface_checker

permissions:
  contents: read

on:
  push:
    branches: [ master, main ]
    paths:
      - 'scarajectory/**/*.py'
      - 'setup.py'
  pull_request:
    branches: [ master, main ]
    paths:
      - 'scarajectory/**/*.py'
      - 'setup.py'

jobs:
  interface_gate:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository code
        uses: actions/checkout@v4
        with:
          submodules: true

      - name: Verify that each concrete class implements a valid interface
        run: |
          python3 gates/gates/interfaces_checker.py scarajectory
